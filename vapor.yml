id: 63769
name: IstAlumniSystem
environments:
    production:
        memory: 1024
        database: final_alumni_system
        cli-memory: 512
        runtime: 'php-8.3:al2'
        build:
            - 'composer install '
            - 'php artisan event:cache'
          # - 'npm ci && npm run build && rm -rf node_modules'
#           deploy:
#       - 'php artisan migrate'
#       -'php artisan db:seed --class=UserRolePermissionSeeder.php
# '